import * as $wml from '../../src';


export const sven = () => (___view: $wml.View) => $wml.map(b, function _map(a) {
    return $wml.domify(b)
}, function otherwise() {
    return document.createDocumentFragment();
});